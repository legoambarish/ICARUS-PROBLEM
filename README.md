# OBC Round 2 — Submission

This branch (`fault-injection`) branches from `solution` and adds one deliberate fault-injection trigger plus a predicted crash tick in `fault_manifest.json`.

See [`DOCUMENTATION.md`](./DOCUMENTATION.md) for the full write-up: every issue found and fixed, plus the fault-injection mechanism and crash-tick reasoning.

The `solution` branch contains the fully corrected code with nothing else added on top.
