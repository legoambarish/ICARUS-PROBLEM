# OBC Round 2 — Submission

This branch (`solution`) contains the fully corrected flight-software simulation, with nothing else added on top.

See [`DOCUMENTATION.md`](./DOCUMENTATION.md) for the full write-up of every issue found and fixed.

The `fault-injection` branch, branched from this one, adds a single deliberate fault-injection trigger plus the corresponding crash-tick prediction in `fault_manifest.json`, documented separately in that branch's `DOCUMENTATION.md`.
